{
  "name": "web-dev-class",
  "version": "1.0.0",
  "description": "## Web Dev Spring 2023",
  "repository": {
    "type":"git",
    "url":"git+https://github.com/draktovus/web-dev-class.git"
  },
  "license": "BSD",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "dev": "nodemon server/index.js --watch server",
    "buildserver": "npm i -ws && npm run build -ws"
  },
  "workspaces":[
    "client"
  ],
  "private":true,
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
